const Navbar = () => {
    return ( 
        <nav className="navbar">
            <h1>Kachi's portfolio site</h1>
            <div className="links">
                <a href={}>Home</a>
                <a href={}>About</a>
                <a href={}>Ho</a>
            </div>
        </nav>
     );
}
 
export default Navbar;